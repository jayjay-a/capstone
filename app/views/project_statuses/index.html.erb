<p id="notice"><%= notice %></p>

<h1>Project Statuses</h1>

<table class="responsive-table">
  <thead>
    <tr>
      <th><%= sort_link(@search, :project_status_description) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @project_statuses.each do |project_status| %>
      <tr>
        <td><%= project_status.project_status_description %></td>
        <td><%= link_to 'Show', project_status %></td>
        <td><%= link_to 'Edit', edit_project_status_path(project_status) %></td>
        <td><%= link_to 'Delete', project_status, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project Status', new_project_status_path %>
