<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.number_field :project_id, id: :job_project_id %>
  </div>

  <div class="field">
    <%= form.label :job_type_id %>
    <%= form.number_field :job_type_id, id: :job_job_type_id %>
  </div>

  <div class="field">
    <%= form.label :job_status_id %>
    <%= form.number_field :job_status_id, id: :job_job_status_id %>
  </div>

  <div class="field">
    <%= form.label :job_start_date %>
    <%= form.date_select :job_start_date, id: :job_job_start_date %>
  </div>

  <div class="field">
    <%= form.label :job_end_date %>
    <%= form.date_select :job_end_date, id: :job_job_end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
