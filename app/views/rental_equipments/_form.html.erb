<%= form_with(model: rental_equipment, local: true) do |form| %>
  <% if rental_equipment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rental_equipment.errors.count, "error") %> prohibited this rental_equipment from being saved:</h2>

      <ul>
      <% rental_equipment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rental_description, "Rental Equipment Description" %>
    <%= form.text_field :rental_description, id: :rental_equipment_rental_description, required: true %>
  </div>

  <%= button_tag( :class => "btn waves-effect waves-light right") do %>
    Save<i class="material-icons right">save</i>
  <% end %>

<% end %>
