<%= form_with(model: employee, local: true) do |form| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee_status_id %>
    <%= form.number_field :employee_status_id, id: :employee_employee_status_id %>
  </div>

  <div class="field">
    <%= form.label :employee_type_id %>
    <%= form.number_field :employee_type_id, id: :employee_employee_type_id %>
  </div>

  <div class="field">
    <%= form.label :state_id %>
    <%= form.number_field :state_id, id: :employee_state_id %>
  </div>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :employee_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :employee_last_name %>
  </div>

  <div class="field">
    <%= form.label :join_date %>
    <%= form.date_select :join_date, id: :employee_join_date %>
  </div>

  <div class="field">
    <%= form.label :dismiss_date %>
    <%= form.date_select :dismiss_date, id: :employee_dismiss_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
