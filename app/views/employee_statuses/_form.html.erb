<%= form_with(model: employee_status, local: true) do |form| %>
  <% if employee_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_status.errors.count, "error") %> prohibited this employee_status from being saved:</h2>

      <ul>
      <% employee_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field col l6">
      <%= form.label :employee_status_description, "Employee Status Description *" %>
      <%= form.text_field :employee_status_description, id: :employee_status_employee_status_description, required: true %>
    </div>
  </div>

  <%= button_tag( :class => "btn waves-effect waves-light right") do %>
    Save<i class="material-icons right">save</i>
  <% end %>

<% end %>
