<%= form_with(model: subcontractor_status, local: true) do |form| %>
  <% if subcontractor_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcontractor_status.errors.count, "error") %> prohibited this subcontractor_status from being saved:</h2>

      <ul>
      <% subcontractor_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subcontractor_status_description %>
    <%= form.text_field :subcontractor_status_description, id: :subcontractor_status_subcontractor_status_description, required: true %>
  </div>

  <%= button_tag( :class => "btn waves-effect waves-light right") do %>
    Save<i class="material-icons right">save</i>
  <% end %>

<% end %>
