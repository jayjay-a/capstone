<%= form_with(model: materiallist, local: true) do |form| %>
  <% if materiallist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(materiallist.errors.count, "error") %> prohibited this materiallist from being saved:</h2>

      <ul>
      <% materiallist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.number_field :project_id, id: :materiallist_project_id %>
  </div>

  <div class="field">
    <%= form.label :material_id %>
    <%= form.number_field :material_id, id: :materiallist_material_id %>
  </div>

  <div class="field">
    <%= form.label :unit_price %>
    <%= form.text_field :unit_price, id: :materiallist_unit_price %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :materiallist_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
