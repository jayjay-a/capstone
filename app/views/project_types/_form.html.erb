<%= form_with(model: project_type, local: true) do |form| %>
  <% if project_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_type.errors.count, "error") %> prohibited this project_type from being saved:</h2>

      <ul>
      <% project_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field col s6" style="min-width: 200px;">
      <%= form.label :project_type_description %>
      <%= form.text_field :project_type_description, id: :project_type_project_type_description, required: true %>
    </div>
  </div>

  <%= button_tag( :class => "btn waves-effect waves-light right") do %>
    Save<i class="material-icons right">save</i>
  <% end %>

<% end %>
