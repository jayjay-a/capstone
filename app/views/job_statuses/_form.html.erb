<%= form_with(model: job_status, local: true) do |form| %>
  <% if job_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_status.errors.count, "error") %> prohibited this job_status from being saved:</h2>

      <ul>
      <% job_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field col s6" style="min-width: 200px;">
      <%= form.label :job_status_description %>
      <%= form.text_field :job_status_description, id: :job_status_job_status_description, required: true %>
    </div>
  </div>

  <%= button_tag( :class => "btn waves-effect waves-light right") do %>
    Save<i class="material-icons right">save</i>
  <% end %>

<% end %>
