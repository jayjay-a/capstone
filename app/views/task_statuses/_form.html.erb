<%= form_with(model: task_status, local: true) do |form| %>
  <% if task_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_status.errors.count, "error") %> prohibited this task_status from being saved:</h2>

      <ul>
      <% task_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field col s6" style="min-width: 200px;">
      <%= form.label :task_status_description %>
      <%= form.text_field :task_status_description, id: :task_status_task_status_description, required: true %>
    </div>
  </div>

  <%= button_tag( :class => "btn waves-effect waves-light right") do %>
    Save<i class="material-icons right">save</i>
  <% end %>

<% end %>
