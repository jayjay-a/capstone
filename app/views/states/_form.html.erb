<%= form_with(model: state, local: true) do |form| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="field col l5">
      <%= form.label :state_name %>
      <%= form.text_field :state_name, id: :state_state_name, required: true %>
    </div>

    <div class="field col l2">
      <%= form.label :state_abbrev, "Abbreviation" %>
      <%= form.text_field :state_abbrev, id: :state_state_abbrev, required: true %>
    </div>
  </div>

  <%= button_tag( :class => "btn waves-effect waves-light right") do %>
    Save<i class="material-icons right">save</i>
  <% end %>

<% end %>
